name: tiara_fin
description: "Aplikasi Keuangan Tiara Finance - Manage Iuran & Transaksi"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Backend
  firebase_core: ^3.1.0
  cloud_firestore: ^5.0.1
  supabase_flutter: ^2.6.0

  # Media
  image_picker: ^1.1.2

  # Chart & Visualization
  fl_chart: ^0.68.0

  # Utils
  intl: ^0.19.0
  shared_preferences: ^2.2.3
  uuid: ^4.4.0

  # Network & Connectivity
  connectivity_plus: ^6.0.5
  http: ^1.2.0

  # UI Enhancements
  shimmer: ^3.0.0
  pull_to_refresh: ^2.0.0
  flutter_spinkit: ^5.2.1
  cached_network_image: ^3.3.1
  percent_indicator: ^4.2.5
  pdf: ^3.10.0
  printing: ^5.11.1
  url_launcher: ^6.2.5
  flutter_slidable: ^3.0.1
  firebase_messaging: ^15.2.10
  flutter_local_notifications: ^17.2.3
  crypto: ^3.0.7
  google_fonts: ^8.0.0
  flutter_staggered_animations: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
